query AllNetworks($filter: networkFilter) {
  allNetworks(filter: $filter) {
    network {
      name
      slug
      description
      category
      icon
      darkThemeIcon
      logo
      darkThemeLogo
      logoBig
    }
  }
}

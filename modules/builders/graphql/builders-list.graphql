query BuildersList($filter: networkFilter) {
  allNetworks(filter: $filter) {
    builders {
      code
      description
      icon
      id
      lastModified
      links {
        id
        label
        url
      }
      name
      scopes
      skils
      slug
      status
      type
    }
  }
}

query ResourceTemplate($docId: PHID!) {
  ResourceTemplate {
    getDocument(docId: $docId) {
      id
      state {
        id
        title
        description
        thumbnailUrl
        summary
        status
        operatorId
        contentSections {
          content
          displayOrder
          id
          title
        }
      }
    }
  }
}

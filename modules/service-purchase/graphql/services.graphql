query ResourceTemplate($docId: PHID!) {
  ResourceTemplate {
    getDocument(docId: $docId) {
      id
      state {
        id
        title
        description
        thumbnailUrl
        summary
        status
        contentSections {
          content
          displayOrder
          id
          title
        }
      }
    }
  }
}
